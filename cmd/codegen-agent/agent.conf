# CodeGen Agent 配置文件
# 服务端 WebSocket 地址
server_url=ws://blog.guccang.cn:10086/ws/uap
#server_url=ws://localhost:10086/ws/uap

# Agent 名称（默认使用主机名）
agent_name=mac-codegen

# Agent 类型: codegen(编码) / deploy(发布)，默认 codegen
agent_type=codegen

# 认证 token（需与服务端 gateway一致）
auth_token=guccang@blog.guccang.cn

# 工作区路径（逗号分隔，项目必须在这些目录下）
workspaces=E:\githubdesktop\go_blog\cmd,E:\githubdesktop

# Claude CLI 路径（默认 claude）
claude_path=claude

# OpenCode CLI 路径（默认 opencode，取消注释启用）
#opencode_path=opencode

# 最大并发任务数
max_concurrent=3

# 最大对话轮次
max_turns=20

# Settings 配置文件目录（每个 .json 文件是一个模型配置）
# 留空则默认为 agent.conf 同目录下的 settings/
settings_dir=./settings

# claude使用-c的settings配置文件名称
resume_models=default,anthropic
